# FEN exam pipeline — full stack (Airflow + MinIO + Paddle + Selenium)
# Stack đầy đủ cho crawl → classify → OCR demo
name: fen-exam

x-airflow-common: &airflow-common
  build:
    context: .
    dockerfile: docker/airflow/Dockerfile
  env_file:
    - .env
  environment: &airflow-common-env
    AIRFLOW__CORE__EXECUTOR: LocalExecutor
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://admin:admin@postgres/airflow
    AIRFLOW__CORE__FERNET_KEY: ''
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: 'true'
    AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
    AIRFLOW__API__AUTH_BACKENDS: 'airflow.api.auth.backend.basic_auth,airflow.api.auth.backend.session'
    FEN_CONFIG_PATH: /opt/fen-exam/dags/config.ini
    FEN_COMPOSE_PROJECT: fen-exam
    FEN_MINIO_ENDPOINT: http://minio:9000
    AIRFLOW__CORE__DAGS_FOLDER: /opt/fen-exam/dags
    FEN_HOST_PROJECT_DIR: ${FEN_HOST_PROJECT_DIR:-}
  volumes:
    - ./:/opt/fen-exam
    - /var/run/docker.sock:/var/run/docker.sock
  user: "${AIRFLOW_UID:-50000}:0"
  depends_on:
    postgres:
      condition: service_healthy
    minio:
      condition: service_started

services:
  minio:
    image: minio/minio:RELEASE.2024-12-18T13-15-44Z
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-admin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-admin}
    volumes:
      - minio-data:/data

  minio-init:
    image: minio/mc:RELEASE.2024-12-18T13-15-44Z
    depends_on:
      minio:
        condition: service_started
    env_file:
      - .env
    volumes:
      - ./scripts/init_minio.sh:/init_minio.sh:ro
    entrypoint: ["/bin/sh", "/init_minio.sh"]
    restart: "no"

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: airflow
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "admin"]
      interval: 5s
      retries: 5

  airflow-init:
    <<: *airflow-common
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db migrate
        airflow users create \
          --username "${AIRFLOW_USERNAME:-admin}" \
          --password "${AIRFLOW_PASSWORD:-admin}" \
          --firstname FEN --lastname Exam --role Admin --email admin@fen.local || true
        echo "Airflow init done"
    restart: "no"

  airflow-webserver:
    <<: *airflow-common
    command: webserver
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
    depends_on:
      airflow-init:
        condition: service_completed_successfully

  airflow-scheduler:
    <<: *airflow-common
    command: scheduler
    depends_on:
      airflow-init:
        condition: service_completed_successfully

  paddle-ocr:
    build:
      context: ./docker/paddle-ocr
      dockerfile: Dockerfile
    ports:
      - "8088:8080"
    environment:
      PADDLE_MAX_IMAGE_SIDE: "2400"

  selenium-chrome:
    image: selenium/standalone-chrome:4.27.0
    ports:
      - "4444:4444"
    shm_size: "2gb"
    volumes:
      - selenium-profile:/data/chrome-profile
    environment:
      SE_NODE_MAX_SESSIONS: "2"
      SE_NODE_OVERRIDE_MAX_SESSIONS: "true"

  fen-job:
    profiles: ["job"]
    image: fen-exam-fen-job
    build:
      context: .
      dockerfile: docker/fen-job/Dockerfile
    env_file:
      - .env
    environment:
      FEN_CONFIG_PATH: /opt/fen-exam/dags/config.ini
      FEN_MINIO_ENDPOINT: http://minio:9000
      FEN_RUNTIME: docker
      FEN_SKIP_LOCAL_OUTPUT: "true"
      FEN_PATHS_OUTPUT_DIR: /tmp/fen-output
      SELENIUM_REMOTE_URL: http://selenium-chrome:4444/wd/hub
      PADDLE_SERVICE_URL: http://paddle-ocr:8080
    volumes:
      - ./dags:/opt/fen-exam/dags:ro
    depends_on:
      - minio
      - paddle-ocr

volumes:
  minio-data:
  postgres-data:
  selenium-profile:
